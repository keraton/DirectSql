'use strict';

describe('myApp.viewQueryInfo module', function() {

  beforeEach(module('myApp.viewQueryInfo'));

  describe('viewQueryInfo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('QueryInfoCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});