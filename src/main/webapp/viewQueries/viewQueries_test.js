'use strict';

describe('myApp.viewQueries module', function() {

  beforeEach(module('myApp.viewQueries'));

  describe('viewQueries controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewQueries');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});